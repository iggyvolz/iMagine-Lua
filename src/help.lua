if _ == nil then
  function _(str)
    return str
  end
end
function help()
  return {_("Please see http://iggyvolz.github.io/iMagine/documentation.html for help"),_("NOTE: still in development so this documentation is likely irrelevant")}
end
